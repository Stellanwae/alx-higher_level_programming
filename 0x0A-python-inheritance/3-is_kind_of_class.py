#!/usr/bin/python3
"""
Function that returns true if the object is an instance of a class
"""


def is_kind_of_a_class(obj, a_class):
    """will return true if is obj instance otherwise false"""
    return (isinstance(obj, a_class))
